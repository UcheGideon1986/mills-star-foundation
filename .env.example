# Cloudinary Configuration
# Get these from your Cloudinary dashboard: https://cloudinary.com/console

# Your Cloudinary cloud name (found in Dashboard)
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name_here

# Your upload preset (create in Settings → Upload → Upload presets)
# Make sure it's set to "Unsigned" for browser uploads
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual Cloudinary credentials
# 3. Never commit .env to git (it's in .gitignore)
